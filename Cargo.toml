[package]
name = "streame"
version = "0.1.0"
edition = "2021"
description = "Régie vidéo légère : caméra/son d'un téléphone en WebRTC → sortie HDMI d'un Mac, scènes, multiview, Stream Deck"
license = "MIT"

[dependencies]
# GStreamer
gst = { package = "gstreamer", version = "0.25" }
gst-app = { package = "gstreamer-app", version = "0.25" }
gst-video = { package = "gstreamer-video", version = "0.25" }
gst-audio = { package = "gstreamer-audio", version = "0.25" }
gst-webrtc = { package = "gstreamer-webrtc", version = "0.25" }
gst-sdp = { package = "gstreamer-sdp", version = "0.25" }

# Serveur web + signaling
tokio = { version = "1", features = ["full"] }
tokio-util = "0.7"
axum = { version = "0.8", features = ["ws"] }
axum-server = { version = "0.7", features = ["tls-rustls"] }
rustls = { version = "0.23", default-features = false, features = ["ring", "std"] }
rcgen = "0.13"
futures-util = "0.3"
local-ip-address = "0.6"
qrcode = { version = "0.14", default-features = false }

# Fenêtres (sortie HDMI + multiview) et rendu GPU (Metal sur macOS via wgpu)
winit = "0.30"
wgpu = "30"
pollster = "1"
bytemuck = { version = "1", features = ["derive"] }

# Stream Deck
elgato-streamdeck = "0.13"
image = { version = "0.25", default-features = false, features = ["png", "jpeg"] }
ab_glyph = "0.2"

# Divers
serde = { version = "1", features = ["derive"] }
serde_json = "1"
toml = "0.8"
anyhow = "1"
clap = { version = "4", features = ["derive"] }
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }

[target.'cfg(target_os = "macos")'.dependencies]
# Sortie audio multicanal directe (CoreAudio) : osxaudiosink de GStreamer se limite à 2 canaux.
cpal = "0.15"
# Tampon sans verrou entre GStreamer et le thread temps réel CoreAudio (évite les trous audio).
ringbuf = "0.4"
# Nom réel des écrans (NSScreen.localizedName) ; mêmes versions que winit.
objc2-app-kit = { version = "0.2.2", features = ["NSScreen"] }
objc2-foundation = { version = "0.2.2", features = ["NSString"] }

[profile.release]
opt-level = 3
debug = false
